spring:
  application:
    name: dan-pretrade-service
  datasource:
    url: jdbc:mariadb://mariadb:3306/dan_pretrade
    username: root
    password: LNqRhw5m1w
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect
  kafka:
    consumer:
      properties:
        spring.json.trusted.packages: com.danservice.*

dan:
  topic:
    client-order: dan-client-orders